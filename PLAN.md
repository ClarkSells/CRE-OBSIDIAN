# STRIVE Navigator Build Plan

1. Establish the Obsidian plugin build and vault-safe record foundation.
2. Implement structured Markdown creation, indexing, relationships, validation, sample data, and RealNex CSV export.
3. Add every required command and premium custom view.
4. Create a loadable sample vault, documentation, and deterministic acceptance checks.
5. Build, install into the sample vault, verify generated records/CSV, and record limitations.

